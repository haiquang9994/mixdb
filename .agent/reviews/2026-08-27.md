# Review 2026-08-27

**Scope:** the whole repository at `24be307` (v0.0.18, 214 commits on `master`) — `src/`,
`src-tauri/src/`, the scripts, the workflows and the `.agent/` notes.
**Method:** four independent read-throughs (Rust backend; shell/core/components; the three
modules; hygiene and docs), each finding re-read at the cited line before it was kept. The
suspicious pure-logic cases were executed. Baseline on this machine: `npm run build` clean
(one 1.8 MB chunk warning), `npm test` 51 files / 758 tests green, `cargo check` and
`cargo clippy` clean, `cargo test` 122 passed / 4 ignored.

**Overall:** the project is in good shape — the module boundary really holds, identifiers and
values never reach SQL unquoted, credentials never reach a command line, and the async guards
in the newer code (schema cache, tunnel banner, query draft) are the kind most apps lack. The
open problems are concentrated in three places: connection and session **lifecycle at the
edges** (cancel during connect, shell exits on its own, script state left on a pooled
connection), **PostgreSQL-specific** gaps in the SQL guard and splitter, and **duplication** that
has passed the "third copy" threshold the notes themselves set. There is no CI that runs any
of the tests.

**Read these first:** R1 (CSP breaks the 0.0.18 headline feature in the packaged build), R2
(`EXPLAIN ANALYZE` bypasses the read-only guard), R3 (script leaves a transaction on a pooled
connection), R4 (SSH session leaks when the shell exits), R6 (connect cannot be cancelled).

## Bugs

- [x] **R1** `src/modules/rest/components/HtmlPreview/HtmlPreview.tsx:59` (`sandbox`, `srcDoc`)
  vs `src-tauri/tauri.conf.json:25` (`csp`). An `about:srcdoc` document inherits the embedder's
  CSP. Production CSP is `script-src 'self'` with no `'unsafe-inline'` and `img-src 'self' data:`,
  so in the packaged app the page's inline scripts and its external images/stylesheets are
  blocked no matter which switch is on; `devCsp` has `'unsafe-inline'`, which is why `dev:app`
  shows it working. Commit `6f55248` did not touch the CSP. *Verify on a packaged build before
  fixing; the fix is a `<meta http-equiv="Content-Security-Policy">` injected into the srcdoc or a
  looser CSP scoped to the frame, not `'unsafe-inline'` for the app.* Also `:36` only injects
  `<base>` when the document has a `<head>`.
  **Fixed.** Confirmed on a packaged build over CDP: the app CSP arrives as `script-src 'self'` plus
  Tauri's two hashes, and the frame's inline script is blocked. The `<meta>` half of the suggestion
  above does **not** work — CSP policies intersect, so a policy delivered inside the frame only ever
  tightens, and `data:`/`blob:` frames inherit the same way. The one document that carries a policy
  of its own is a served one, so `src-tauri/src/modules/rest/preview.rs` registers a `mixdb-preview`
  scheme and answers with the response body under a CSP the pane's two switches pick; `frame-src`
  names that scheme and nothing else. `img-src` gained `blob:` in the same edit — the Preview tab's
  image body is a blob URL and was blocked in the packaged build for the same reason.
- [x] **R2** `src/modules/db/sql/guard.ts:263-265` (`READ_VERBS`), `:287-326`
  (`writingStatements`), `:148` (`unguardedWrites`). `EXPLAIN` is a read verb unconditionally, but
  `EXPLAIN ANALYZE <stmt>` executes the statement on PostgreSQL (always) and MySQL ≥ 8.0.18.
  Executed: `writingStatements("EXPLAIN ANALYZE DELETE FROM users")` → `[]` on both dialects. On a
  read-only connection this deletes the table without a prompt.
  **Fixed.** Both gates now judge an `EXPLAIN ANALYZE` by the statement it is given: `judged()` reads
  past `EXPLAIN` and its options and hands the inner statement to `writingStatements` and to
  `unguardedWrites`, so `EXPLAIN ANALYZE DELETE FROM users` is refused as a `DELETE` and asked about
  as one. Whether it analyses is decided over the statement's own tokens rather than the top-level
  ones, because PostgreSQL's `EXPLAIN (ANALYZE, VERBOSE) ...` hides the word inside brackets that
  `topLevelWords` drops; `ANALYZE`/`ANALYSE` are reserved in both dialects, so an unquoted one can
  only be the option. A plain `EXPLAIN DELETE` still passes — it plans and runs nothing. Cases in
  `guard.test.ts` for both dialects, both spellings and the CTE behind one.
- [x] **R3** `src-tauri/src/modules/db/drivers/mysql_script.rs:325-345` (`run`, the `USE` at
  `:341`), `postgres_script.rs:339-427` (`run`), `mysql.rs:78-85` (`query`), `mysql_script.rs:257-301`
  (`validate`: `SET @mixdb_check`, `PREPARE`). A script runs on `pool.acquire()` and the connection
  goes back with whatever the script left: an open `BEGIN`, a `USE other_db`, `SET autocommit=0`,
  `LOCK TABLES`, temp tables. sqlx only pings on return. `BEGIN; SELECT 1/0;` on PostgreSQL leaves
  an aborted transaction that the next `list_tables`/`table_data` on that connection fails with
  (`25P02`); an uncommitted `UPDATE` on MySQL holds row locks on an idle connection until a later
  `pool.begin()` implicitly commits it. Run scripts on a detached connection, or roll back /
  close when `in_transaction()` after the script.
  **Fixed.** Both `run`s now call `PoolConnection::close_on_drop()` on the connection they acquire, so
  the script's session is closed when the run ends instead of going back to the pool. On drop rather
  than at the end, which covers the error paths and the run being dropped because the tab closed; the
  pool keeps the permit meanwhile, so this never opens a sixth connection. Neither server has a
  statement that would do instead — MySQL has no session reset at all, and PostgreSQL's `DISCARD ALL`
  needs a connection that is not mid-transaction, which is the case that has gone wrong. The cost is
  one handshake per Run.
  Reproduced against the test servers with a `max_connections(1)` pool, then re-run with the fix
  removed: PostgreSQL gave `25P02 current transaction is aborted` on the query after
  `BEGIN; SELECT 1/0;`, MySQL gave back `42` for `SELECT @mixdb_r3` after a script had set it. Both
  pass with the fix. The checks were temporary — they need a live server, so nothing was committed.
  `validate` still keeps its pooled connection on purpose: it fires on a debounce while someone
  types, and what it leaves (the `USE`, `@mixdb_check`) is bounded and read by nothing — every other
  query names its database in full or binds it to `information_schema`. Noted in its doc comment.
  `mysql.rs:78-85` needed no change: `query` issues its own `USE`, and it has no callers today.
- [x] **R4** `src-tauri/src/modules/terminal/remote.rs:36-63` (read task), `:67-92` (write task
  holding the SSH `Handle`), `local.rs:293-328`, `state.rs:19-25` (`Session::drop`),
  `commands.rs:96` (the only `sessions.remove`). When the shell exits on its own the read task
  ends but nobody cancels `kill`, so the write task — and the SSH connection with its 15 s
  keepalive — lives until `Session` is dropped; and `TerminalView.tsx:208` skips `closeSession`
  once `ended`, so the `Session` stays in the map for the life of the app. Type `exit` in an SSH
  tab: TCP session and keepalive run forever; local shells leave two blocked threads and a task
  per closed tab. Cancel `kill` when the read task ends and remove the entry on `Exit`.
  **Fixed.** Removing the entry on `Exit` turned out to cover both halves: `Session::drop` already
  cancels `kill` *and* releases the two senders, and it is releasing the senders — not the token —
  that wakes the local write and resize threads, which block on `blocking_recv()` and would not have
  noticed a cancelled token. So the sink now calls `TerminalState::forget(id)` when it emits `Exit`,
  and that one call ends the SSH write task (closing the channel, the `client::Handle` and the
  keepalive with it), the local write/resize threads and the killer task.
  Two details: the removed `Session` is dropped outside the lock, and `terminal_open` re-checks an
  `ended` flag under the same lock right after inserting — a shell that dies instantly emits `Exit`
  before `spawn` returns, and without the re-check that dead session would be inserted after the
  removal had already run and nothing would ever take it out again.
  `output_sink` came out of `terminal_open` so a test can drive the real sink: building a mock
  `AppHandle` fails on this machine (`tauri`'s `test` feature makes the test binary die with
  `STATUS_ENTRYPOINT_NOT_FOUND` before any test runs), and the only line left outside the test is
  the one that pulls `TerminalState` off the handle. `commands.rs` test
  `a_session_that_ends_by_itself_leaves_the_map` spawns `exit 3` and waits for the map to empty; it
  fails as expected with the `forget()` call removed.
  `TerminalView.tsx:208` needed no change: skipping `closeSession` once `ended` is now correct, and
  the doc comment on `terminal_close` says so.
- [x] **R5** `src/modules/terminal/components/TerminalView/TerminalView.tsx:181-215` (open
  effect + cleanup) vs `src-tauri/src/modules/terminal/commands.rs:26-55` (`terminal_open`
  inserts only after `spawn(...).await`). Close the tab during the SSH handshake: cleanup calls
  `closeSession(id)` → `terminalUnknownSession` (swallowed) → spawn finishes and inserts → pty/SSH
  leaks and the channel keeps writing into a disposed xterm. A `disposed` flag checked in the
  `.then()` that calls `closeSession` late fixes it.
  **Fixed.** A `disposed` flag set at the top of the cleanup, checked in all three places the effect
  can be spoken to after it is gone. In `.then()` it calls `closeSession(id)` late — the only moment
  anyone still knows the session exists, since the backend inserts it after the invoke resolves and
  the cleanup's own `closeSession` ran while the map was still empty.
  The other two guards are the same race, less visible. The effect's deps are `[target]`, so it also
  re-runs on a target change while the tab lives on: without the guard the message callback writes
  into a disposed `Terminal` (which throws) and reports the *old* session's `Exit` onto the tab the
  new run just built, and `.catch` sends the old run's `onFailed` to the same place.
  Not covered by a test: the project has no component-test setup — no jsdom, no testing-library, no
  `.test.tsx` — and adding one for a single effect race is more than this is worth. Checked by
  reading, with `tsc` and the 775 tests green.
- [x] **R6** `src/modules/db/DbTab.tsx:559-588` (`connect`), `:215-223` (unmount cleanup), `:896`
  (Connect button). `connect_db` in flight has no cancel flag; cleanup reads
  `connectionIdRef.current`, still `null`, so a tab closed while connecting over a slow tunnel
  leaves pool + tunnel in `DbState` until exit. The button is not disabled while connecting, so a
  double click makes two connections and the first id is overwritten and never disconnected.
  **Fixed.** Both halves are the same shape as R5 and are fixed the same way. A `closedRef` set at
  the top of the unmount cleanup, read where `connect`'s await comes back: if the tab is gone it
  calls `disconnect_db` with the id it just received and returns. That is the only moment anyone
  knows the id — the cleanup ran while `connectionIdRef.current` was still `null` and nothing else
  is ever told. The `catch` gets the same guard, so a failure arriving after the tab closed does
  not set state on a dead component. For the second dial, `connectingRef` (checked and set before
  the await, cleared in a `finally`) plus `connecting` state on the button's `disabled`: the ref
  and not the state alone because the two ways in are not both clicks — `openAndConnect` runs from
  the restore effect and from a saved row's double click, and a state set moments earlier is not
  visible in either. Not covered by a test, same reason as R5: no component-test setup in the repo.
  Checked by reading, with `tsc` clean and the 775 tests green; `disconnect_db` treats an id it has
  already forgotten as a no-op, so the late call is safe on every path.
  *Follow-up (same day): the first version of this broke connecting outright in dev.* `closedRef`
  outlives the effect, and StrictMode throws the first mount away — setup, cleanup, setup — so a
  flag only ever *raised* was left raised on a live tab, and every connection it opened was hung up
  on the moment it arrived: TCP and SSH alike sat on "Connecting…" for ever. It is lowered in the
  effect's setup now. R5's twin was never exposed to this because its flag is a `let` inside the
  effect and its session id is minted per run. Reading the form was also moved above the
  `connectingRef` assignment, so nothing can throw between the flag going up and the `try` that
  lowers it — the same symptom by a different road.
- [x] **R7** `src/modules/db/sql/statements.ts:126-147` (`splitStatements`), `lint.ts:154-186`
  (`tokenize`), `syntax.ts:64` (`backslashEscapes: false`), `syntax.ts:685-696` (`dollarTag`), and
  the Rust twin `src-tauri/src/modules/db/drivers/postgres_script.rs:174-193`. PostgreSQL `E'...'`
  literals do use backslash escapes on both sides: `SELECT E'it\'s; here'; SELECT 2` splits into
  two broken statements. `dollarTag` also accepts a `$` inside an identifier (`a$b$c`, legal in
  PostgreSQL) as a dollar-quote opener and swallows the rest of the script. Neither case is in
  `statements.test.ts` or the Rust tests.
  **Fixed.** Both are now what the server does, checked against PostgreSQL 5432 before anything was
  written: `SELECT E'it\'s; here'` comes back as `it's; here` while `SELECT 'a\b'` comes back with the
  backslash intact, so escaping belongs to the *literal* and not to the engine — a new `escapeStringPrefix`
  flag on `SqlSyntax` and an `opensEscapeString` helper, read once per literal in `statements.ts`,
  `lint.ts` and the Rust twin. The prefix has to be touching and cannot be the tail of a longer name,
  which is why `type'a\'` still splits where `e'a\'` does not.
  For the second, the server named the column in `SELECT 1 AS a$b$c` exactly that, and answered
  `SELECT$$hi$$` with a syntax error quoting the whole of `SELECT$$hi$$` — PostgreSQL takes the longest
  identifier it can before it looks for a quote, `$` being legal inside one. So `dollarTag`/`dollar_tag`
  now refuse a `$` glued to an identifier character, which is the same rule in one line and covers
  `lint.ts` for free. Two tests each side, both proven to fail with the fix off; 777 frontend tests and
  132 Rust tests green, clippy clean. `U&'...'` is left alone: a backslash there is a unicode escape and
  any `\'` in one is a server error whichever way the splitter reads it.
- [x] **R8** `src-tauri/src/ssh/mod.rs:450-453` (`open_tunnel`, `listener.accept()` →
  `Err(_) => break`). A transient accept error (`WSAECONNRESET`/`ECONNABORTED` on Windows when the
  pool drops a half-open socket, `EMFILE` elsewhere) ends the task and drops the listener; every
  later query fails with `connectionLost` while the watcher, which only checks the SSH handle,
  shows no banner. `continue` with a short sleep on transient errors.
  **Fixed.** Split in two. An error that belongs to one connection — `is_transient_accept`:
  `ConnectionAborted`/`ConnectionReset`/`ConnectionRefused`/`Interrupted` — is not the listener's
  business at all, so the loop just takes the next one, with no wait and nothing counted. Anything
  else waits `ACCEPT_RETRY` (100 ms) and tries again, **without ever giving up**: the port is bound
  once, in `open_tunnel`, so nothing downstream can reopen it, and a file-descriptor squeeze is a
  thing that passes. That is the same bargain the watcher above it already makes, and the loop still
  lives exactly as long as the `Tunnel` — its `Drop` aborts the task. What the review's `break` cost
  was the silence, so at `ACCEPT_ALARM` (20 in a row, ≈2 s of a deaf port) the tunnel says
  `TunnelEvent::Failed` once, which is the banner; a later successful accept says `Reconnected` and
  takes it down again. New key `error.tunnelAcceptFailed` in both dictionaries, worded so the user
  knows MixDB is still trying.
  Tested where it is worth testing: the classifier, by raw OS code rather than by hand-written
  `ErrorKind`, because the claim being made is that *Windows' and Unix's own numbers land in the
  kinds the loop matches* — `10054`/`10053` yes and `10024` no on this machine, `104`/`103`/`24`
  under `#[cfg(unix)]` for CI. The loop itself needs a real SSH server and a real `EMFILE` to drive,
  so it is not covered. 133 Rust tests and 777 frontend tests green, clippy clean.
  One caveat left standing: the banner's *Retry* calls `tunnel_reconnect`, which reopens the SSH
  session and does nothing for a deaf local port. The message says so in words rather than pretending
  the button will help.
- [x] **R9** `src/components/Select/Select.tsx:297-302` (`onKeyDown`, `"Escape"`) with every dialog's
  window-level Escape listener (`SettingsModal.tsx:70-76`, `ConfirmDialog.tsx:44-50`,
  `NameDialog.tsx:65-73`, `CellDialog.tsx:69-75`, the module dialogs). Select calls
  `preventDefault` but not `stopPropagation`, and no dialog checks `defaultPrevented`: Escape to
  close the "Cursor style" dropdown in Settings closes Settings; Escape on the collation/type
  dropdown in `ColumnDialog`/`IndexDialog` throws the form away.
  **Fixed.** Both halves, because they cover different things. `Select` now calls
  `stopPropagation` beside its `preventDefault`, and that alone settles the reported bug: React's
  listener sits on the root container, so stopping there stops the event before `window` — where
  every dialog listens — ever hears it. The second half is the contract, and it is what stops the
  next control repeating this: `isUnhandledEscape` in `dialogMotion.ts`, `e.key === "Escape" &&
  !e.defaultPrevented`, now read by all thirteen window-level dialog listeners. `defaultPrevented`
  rather than a list of elements to exclude — it is the one signal that means somebody already dealt
  with the press, and it costs them nothing to raise. `InsertDocumentsDialog` had written that check
  out by hand already, which is the precedent; it now shares the helper and keeps only its own
  input/textarea rule, since a rename field takes Escape without marking it.
  Deliberately left alone: `ContextMenu`, `Tooltip`, `TunnelBanner` and the grid's own Escape
  handlers. No `Select` is ever rendered inside any of them, checked, so bringing them in would be
  changing behaviour with no bug behind it.
  `isUnhandledEscape` has a test; the dialogs themselves do not, for the same reason as R5 and R6 —
  no component-test setup in the repo. 780 frontend tests, `tsc`, `npm run build` and the module
  boundary check all green.
- [x] **R10** `src/shell/update.ts:328-373` (`check`), `UpdateSection.tsx:28` (`working`). "Check
  now" is enabled while `status === "downloaded"` and `check()` closes the found handle: the
  downloaded bundle is discarded and status falls back to `available`. `pending` (`:293`) also
  omits `checking`, so the brand dot and toast flicker off and on during a re-check.
  **Fixed.** Both, and both by moving the rule to where the facts are. `UpdateSection` was working
  out for itself whether a check was safe, from a `status` it can see — but the reason it is unsafe
  is the plugin's handle, which only the hook holds, so the hook answers now: `canCheck` on
  `UpdateCheck`, and the button is `disabled={!update.canCheck}`. Behind it `holdsUpdate` counts
  `downloaded` alongside `downloading` and `installing` — a bundle on disk is a handle in use just
  as much as one still arriving, and `check` closes it.
  `isPending` now lists `checking` too. A re-check does not un-find what was found: the release is
  still the release and the handle is still there, so the dot and the panel stay put instead of
  blinking off for the length of the request. `hasRelease` still carries the other half of the old
  comment — a check that failed before finding anything is Settings' business, not a panel over the
  user's work.
  Both are pure functions now and both have tests, including the negative sweep over every
  `UpdateStatus`; `update.ts` imports cleanly under the node test environment, so no sibling module
  was needed. 786 frontend tests, `tsc`, and the boundary check green.
- [x] **R11** `src/components/TabStrip/useTabReorder.ts:114` (unmount effect), `:142-158` (`tick`).
  Unmount mid-drag aborts the listeners but not `cancelAnimationFrame(h.frame)` nor `held.current`,
  so `tick` reschedules itself forever on a detached strip (`getBoundingClientRect()` → 0). Ctrl+W
  while dragging a REST request tab triggers it.
  **Fixed.** The unmount was doing a third of what `release` does, and the two-thirds it left out
  were the two that matter to `tick` — which reads `held.current`, not the listeners, and so kept
  booking the next frame for ever. Rather than repeat the teardown in two places, `release`'s first
  four lines are now `letGo()`: null the ref, abort the listeners, cancel the frame, hand back what
  was held. `release` calls it and carries on with the drop animation; the unmount calls it and
  stops there, which is the whole difference between the two. `frame: 0` before the first drag is
  fine — `cancelAnimationFrame(0)` does nothing.
  Not covered by a test: it needs a real DOM, a pointer sequence and an unmount, and the repo has no
  component-test setup. `tsc`, 786 tests and the boundary check green. Left alone deliberately: with
  a strip of zero width `near` is 0 and `over` is `Infinity` even while mounted — harmless, since
  `scrollLeft` then clamps to 0 and nothing follows, and no bug in the review stands on it.
- [x] **R12** `src/modules/rest/parsePaste.ts:153-167` (`SKIPPED_WITH_VALUE`), `:197-204`
  (`looksLikeUrl`), `:363`. Value-taking curl flags missing from the list (`-c/--cookie-jar`,
  `-T`, `--cert`, `--key`, `--cacert`, `-m`, `-w`, `-D`, `--resolve`, `--data-urlencode`, `--json`)
  push their value into `bare`. Executed: `curl -c cookies.txt localhost:3000/login` → url
  `cookies.txt`; `--json '{"a":1}'` and `--data-urlencode 'q=a b'` → body `none`, silently.
  **Fixed**, and the behaviour was measured rather than read: curl 8.21 through a local echo server,
  every expectation in the new tests taken from what actually went over the wire.
  The skip list is now long and dull on purpose — output, cookies, identity, proxy, certificates,
  timeouts, ranges, config — since a flag left out of it does not fail loudly, it quietly hands its
  value to `looksLikeUrl`, which is happy with any `word.word`.
  The two that carry a body are parsed rather than skipped. `--json` is `--data-binary` plus
  `Content-Type` and `Accept`, both added *after* the walk so a `-H` in the command keeps the field —
  which is curl's own answer: with an explicit `Content-Type: text/plain` it sends `text/plain` and
  adds `Accept: application/json` beside it. `--data-urlencode` encodes the way curl encodes, which
  is not `encodeURIComponent`: space is `+`, and `!'()*` are escaped, leaving the RFC 3986 unreserved
  set — `q=a b&c` really does go out as `q=a+b%26c`. Its five spellings look for `=` before `@`, so
  `x=y@z` is a value and not a filename (curl answers `x=y%40z`); the two `@file` forms are eaten
  without a body, since there is no file to read from here.
  Two beyond the finding, both one line and both measured: `-T` is a PUT (the file cannot be read,
  but the verb can still be right), and `--form-string` is `-F` with the value taken literally.
  Nine tests, `tsc`, 795 tests and the boundary check green. `looksLikeUrl` itself is left as it is:
  tightening it would mean guessing which dotted words are hostnames, and with the list complete
  nothing reaches it that should not.
- [x] **R13** Async results applied to the wrong database. `src/modules/db/sql/SqlWorkspace.tsx:500-507`
  (`listTables`) and `mongo/MongoWorkspace.tsx:395-402` (`listCollections`) have no cancel flag
  while the `[selectedDb]` effect beside them does, and the `.catch` at `:431` is not gated
  either; `redis/RedisWorkspace.tsx:349-363` (`loadMoreKeys`) appends a page of db 0 onto db 1
  and carries db 0's cursor/`scanDone` with it; `SqlTable.tsx:770` reports a stale table's error
  onto the table now shown (`TableStructure.tsx:213-218` does it right).
  **Fixed**, all four, and the first three by the same move: a run counter in a ref rather than a
  `cancelled` flag. The flag was never going to be enough here — it only lets a caller invalidate
  *itself*, and the thing that goes stale is started somewhere else. `tablesRun` in `SqlWorkspace`
  and `collectionsRun` in `MongoWorkspace` are bumped by the `[selectedDb]` effect *and* by
  `listTables`/`listCollections`, so each invalidates the other; `scanRun` in `RedisWorkspace`
  replaces the sweep's own `cancelled` for the same reason, since the thing to invalidate —
  `loadMoreKeys` — lives outside the effect. `SqlTable`'s `.catch` just needed the guard its
  neighbours already had, the way `TableStructure` writes it.
  The spinners were the part the finding did not mention and the fix would have broken: a
  superseded read's `finally` no longer belongs to the current run, so nothing would ever put
  `tablesLoading`/`collectionsLoading`/`loadingMore` out again. Each effect now clears its own as it
  starts — a database change ends whatever the sidebar was reading, which is true as well as
  convenient.
  Left alone: `RedisWorkspace:289` (`loadDatabases`), which turns on `connectionId` alone and so has
  no database to be stale about. No tests — component races, and no component-test setup; checked by
  reading each of the four against its own effect's deps. `tsc`, 795 tests and the boundary check
  green.
- [x] **R14** `.github/workflows/tool-downloads.yml:96` (`cargo test --lib db::tools`). The test
  module is `modules::db::drivers::tools::tests`; `db::tools` matches nothing (`-- --list` → 0
  tests, exit 0), so the workflow that exists to run the download tests has always passed without
  running them. `:17` and `:28` also cite `src-tauri/src/db/tools.rs`, which does not exist.
  **Fixed**, and confirmed both ways before the edit: `db::tools` really does list 0 tests and exit
  0, `modules::db::drivers::tools` lists 9. The two stale paths in the header are corrected too.
  Changing the filter alone would have left the same trap for the next rename, silent in the same
  way — a green tick meaning "nothing was checked" is worse than no workflow at all. So the step now
  lists the tests first and counts the `_download_still_installs_and_runs` ones; fewer than three and
  it fails, saying to fix the filter rather than the check. `shell: bash` so the same script runs on
  the Windows runner, whose default is PowerShell.
  Checked by running: the shell block under `set -eo pipefail` in Git Bash gives 3 for the new filter
  and 0 for the old, where the guard fails as intended; the YAML parses and the step's fields read
  back correctly. And since this re-arms a check that had never once executed, the tests themselves
  were run here rather than left to find out in CI — 9 passed in 91 s, the three vendor downloads
  included, so the pins are still good on Windows at least. No changelog entry: CI work, per
  `.agent/conventions/changelog.md`.
- [x] **R15** `package-lock.json:3,9` is `0.0.17`; everything else is `0.0.18`.
  `scripts/set-version.mjs` bumps six files but not the lock, so the next `npm install` rewrites
  it and the version diff lands in an unrelated commit. Add it to the script (its comment also
  says "five files" at `:2` and "sixth" at `:15`, and claims `--locked` at `:9`, which
  `release.yml` never passes).
  **Fixed.** The lock is `0.0.18` and `set-version.mjs` patches it, both of the entries it carries —
  the root object and `packages[""]`, which npm keeps equal. Since one file now wants the version
  found *twice*, `patch` takes an expected count and throws unless it finds exactly that many:
  finding it once here would be as half-bumped as not finding it at all, and the whole point of that
  helper is that a silent no-op is the failure. Comments renumbered — six files, changelog seventh.
  Run end to end at `0.0.99` and reverted: all seven written, and the lock diff is exactly the two
  lines. The count guard was tested too, by asking it for three: it stops with the file named.
  The `--locked` half of this finding has expired. It was true at `24be307`, but `934e6db` added
  `ci.yml`, which runs `cargo clippy --locked` and `cargo test --locked` — so "which CI is" at `:9`
  now reads correctly and was left alone. `release.yml` still does not pass it, and does not need to.
  No changelog entry: nothing here is visible to anyone running the app.
- [x] **R16** Docs that contradict the tree. `docs/RELEASING.md:17` "There is no test suite" (there
  are 758 tests) and `:173` `src/update.ts` (it is `src/shell/update.ts`);
  `.agent/notes/query-editor-roadmap.md:79,87,137,220,278` and
  `.agent/decisions/2026-08-11-two-checkers-and-when-they-keep-quiet.md:8` link to
  `src/mysql/{lint,guard,statements,schemaCache}.ts` / `MysqlWorkspace.tsx`, all now under
  `src/modules/db/sql/`; the roadmap also still says the editor is a textarea (`:24`), Run is
  `Ctrl+R` (`:102`) and auto-LIMIT is a 500-row setting (`:211`, it is `AUTO_LIMIT = 10_000` in
  `guard.ts:189`); `.agent/conventions/filter-bar.md:5,24` cites `src/<db>/filters.ts`;
  `.agent/architecture/overview.md:74-75` lists three kinds and no PostgreSQL;
  `src-tauri/Cargo.toml:4-5` `description` omits PostgreSQL and `authors = ["you"]` ships in the
  installer metadata; `AGENT.md:57` describes `<db>/Workspace.tsx, api.ts, filters.ts` but
  `postgres/` has no `filters.ts` and mysql/postgres have no `Workspace.tsx`.
  **Fixed**, each against the tree rather than against the finding. Every path named here was
  checked to exist before it was written down, and the sweep was widened: a script over every
  markdown link in the repo now reports **no dead relative link outside `docs/superpowers/`**.
  `RELEASING.md` had two more of the same kind the finding did not reach — "Six files" and "the
  other five files", both a file behind after R15 — and its check step now names what CI actually
  runs instead of denying a suite that has 795 frontend and 133 Rust tests. `filter-bar.md` was
  wrong in a way worth spelling out rather than patching: there is no per-*database* operator list
  at all, there is one per *kind* — `sql/filters.ts` shared by MySQL and PostgreSQL, and
  `mongo/filters.ts` — and `build_where` is now two functions, since the placeholders differ.
  `AGENT.md`'s `<db>/` line was describing a shape no directory has; it now says what the four
  actually hold. `Cargo.toml` reads back through `cargo metadata` as the corrected description and
  `authors = ["haiquang9994"]` — the GitHub handle and not an address, since this string ships in
  the installer's metadata.
  One claim in the finding does **not** hold: the roadmap's `Ctrl+R` at `:102` is still correct —
  `QueryEditor.tsx` has exactly one way to run and that is it. Left as it was. The roadmap is a
  dated plan, so its stale statements were reframed rather than rewritten: "Today's editor is a
  textarea" became "the editor as this was written", and auto-LIMIT keeps its shipped-at-500
  history with the raise to `AUTO_LIMIT = 10_000` written beside it. Its five broken links were
  simply broken and are simply fixed.
  Left alone: 16 dead links inside `docs/superpowers/plans` and `/specs`. Those are archived
  planning artefacts, and most are off by a directory level — they were never valid, so they are a
  record of how something was planned and not a map anyone can follow into the code.
- [x] **R17** `src/modules/terminal/savedTargets.ts:486-504` (`parseSavedTarget`, `config as
  SshConfig`), `:526-541` (`mergeSecrets`), `:578-587` (`loadSavedTargets`). One hand-edited ssh
  entry without `auth` throws inside `Promise.all` and empties the whole list for the session —
  the opposite of what the comment at `:559-561` promises. Parse `ssh` defensively like `local`.
  **Fixed.** `config as SshConfig` was a promise to the compiler where a check was wanted; it is now
  `parseSshConfig`, reading a field at a time. What it refuses and what it repairs was taken from
  this file's own precedent rather than invented: `host` is the only thing that cannot be guessed,
  so a missing one drops the row the way a missing `shellName` drops a local one — but everything
  else has a right answer, because the file already says a server whose secret is gone still comes
  back with an empty password box. So a missing or unrecognised `auth` reads as an empty password
  rather than throwing the row away with the name and the address on it, a port it cannot believe
  falls back to 22, and a `privatekey` entry stays one.
  Four tests, and they were run against the old file to be sure they discriminate: all four fail
  there, one of them on the exact `TypeError` that used to escape into `Promise.all`. 798 tests
  and `tsc` green.
- [ ] **R18** `src/shell/App.tsx:167-175` (`<Tab>` in the app tab bar). No `role="tab"`,
  `tabIndex` or `onKeyDown={tabKeyDown(...)}`, unlike `RequestTabs.tsx:50-56` and
  `ResponsePane.tsx:238`; `TabStrip` has no `role="tablist"`. The shell tab bar is the only strip
  that cannot be reached or operated from the keyboard (Ctrl+Tab aside).
- [ ] **R19** Small, each a one-line fix:
  `src-tauri/src/modules/db/drivers/mysql.rs:119-128` (`kill_query`) matches the English message
  `"Unknown thread id"` instead of error number 1094 — the way `mysql_script.rs:228-243` already
  does it; `src-tauri/src/modules/db/commands/mod.rs:265-269` (`disconnect_db`) drops pools without
  `close().await`, so servers log aborted connections (`postgres::Pools::close_pool` exists at
  `postgres.rs:114-119` and is never called); `src/modules/db/components/QueryEditor/QueryEditor.tsx:285-304`
  hardcodes `source: "MySQL"` on PostgreSQL server diagnostics; `src/modules/db/DbTab.tsx:472`
  hardcodes `(copy)` (REST uses `t("rest.copySuffix")`) and `:579` passes the raw kind
  (`"mysql"`) to `connection.fallbackTitle`; `DbTab.tsx:328` `setUseSsl(c.use_ssl ?? true)` while a
  new form defaults to `false` (`:173`), so loading a Redis entry then switching to MySQL turns
  SSL on silently; `src/modules/db/queryHistory.ts:470-481` records queries on unsaved
  connections under `profileId: ""` — never visible, never clearable, still consuming the 300
  slots; `src-tauri/src/modules/rest/commands.rs:72-78` turns a non-ASCII header value into `""`
  (`from_utf8_lossy` instead) and `:134` treats `timeout_ms = 0` as an instant timeout.

## Improvements

- [ ] **R20** No CI runs anything on push or PR: not `tsc`, not `vitest`, not `cargo test` (17
  `#[cfg(test)]` modules), not `clippy`/`fmt`, no `cargo audit`/`cargo deny`; `release.yml` builds
  straight to a draft. `.github/workflows/` has only release, update-notes and the (broken, R14)
  tool-downloads. One `check.yml` with the four commands from `AGENT.md` closes this.
- [ ] **R21** No linter. The concrete cost today: `t` frozen inside `useCallback`s with missing
  deps (`SqlWorkspace.tsx:346-357,500-507`, `MongoWorkspace.tsx:319-330`,
  `RedisWorkspace.tsx:285-291`, `ToolsSection.tsx:342-346`) so errors after a language switch
  come out in the old language; and the module boundary is enforced by a hand-run grep
  (`adding-a-module.md:108-112`) that also flags the comment in `shell/shortcuts.ts:33,82`.
  `eslint` with `react-hooks/exhaustive-deps` and `no-restricted-imports` replaces both.
- [ ] **R22** Ten copies of the same store boilerplate (`useSyncExternalStore` + `Store.load` +
  `snapshot/loaded/inFlight/listeners/ensureLoaded/publish/persist`):
  `db/savedConnectionsStore.ts`, `db/queryHistory.ts`, `db/querySnippets.ts`, `db/tools.ts`,
  `rest/requestsStore.ts`, `rest/environmentsStore.ts`, `rest/historyStore.ts`, `rest/workspace.ts`,
  `terminal/savedTargetsStore.ts`, `terminal/settingsStore.ts`. The comments at
  `savedTargetsStore.ts:644-645` and `settingsStore.ts:849-852` already say "third copy → `core/`".
  A `core/jsonStore.ts` `createStore<T>(file, key, {load, defaults})` covers all ten.
- [ ] **R23** The SQL and Mongo workspaces are one near-identical pair: transfer-progress effect
  (`SqlWorkspace.tsx:366-382` / `MongoWorkspace.tsx:339-355`), server-info effect, the database
  picker with `/new`/`/reload` sentinels (`:639-688` / `:540-589`), `schemaTokens`/`bumpTokens`/
  `forget*`/`rowsChanged` (`:178-270` / `:161-242`), list/reload/create/rename/drop handlers, the
  sidebar. `SqlTable`/`NoSqlTable` repeat the filter cache (`SqlTable.tsx:130-162` /
  `NoSqlTable.tsx:77-111`) and their `request.ts` files are ~90 % the same (`:229-344` /
  `:345-451`). The sidebar resizer is hand-written three times (`SqlWorkspace.tsx:272-342`,
  `MongoWorkspace.tsx:244-314`, `RedisWorkspace.tsx:173-266`, ~70 lines each, and they leak
  `document` listeners on unmount mid-drag) while `src/components/Splitter` exists and
  `RestTab.tsx:526-539` uses it. Roughly 300–400 lines to remove; this is what the `sql/` layer
  is for.
- [ ] **R24** Oversized components: `SqlTable.tsx` 1813 lines (grid, editing, selection, menu,
  delete, insert, FK, column measuring), `DbTab.tsx` 1098 (a 20-`useState` form plus the saved list
  plus the workspace switch — `applySavedConnection` `:317-359` and `newConnectionForm` `:361-387`
  each call 17 setters), `TableStructure.tsx` 907, `QueryEditor.tsx` 826, `DocumentNode.tsx` 801,
  `MongoWorkspace.tsx` 792. `DbTab` first: a `formStateFrom(config | null)` + `useReducer`, and
  `ConnectionForm` out of the tab.
- [ ] **R25** SSH secret split/merge exists twice — `db/savedConnections.ts:37-86` and
  `terminal/savedTargets.ts:507-546` — and `SshConfig`/`SshAuth` are declared twice
  (`db/types.ts:619-628`, `terminal/types.ts:408-419`). Nothing in them is module-specific;
  they qualify for `core/` under `frontend.md`'s own rule. Related: `PRIVATE_KEY_PLACEHOLDER`
  computed two ways (`DbTab.tsx:142-146` via `core/platform`, `TargetForm.tsx:26-30` via
  `navigator.userAgent`) with different default key names.
- [ ] **R26** Dialogs: 13 copies of the window-level Escape effect and the
  `overlay`/`dialog role="dialog" aria-modal` markup (`CellDialog`, `ConfirmDialog`, `NameDialog`,
  `SettingsModal` and nine module dialogs). No dialog traps focus or restores it on close;
  `SettingsModal` and `CellDialog` focus nothing on open; `ConfirmDialog.tsx:70` auto-focuses the
  confirm button even when `danger` (Enter deletes); `ErrorBanner`/`LoadingOverlay` have no
  `role="alert"`/`"status"`; `ContextMenu.tsx:73` has no `role="menu"`, no focus, no arrow keys.
  A `<Modal>` primitive (or `useDialogExit(onEscape)` grown to own Escape, `defaultPrevented`,
  focus) fixes R9 in one place and every future dialog with it.
- [ ] **R27** Destructive red is hardcoded in three variants across the shared layer — `#d32f2f`
  (`ActionBar.module.css:35`, `ErrorBanner.module.css:7,25,52`, `App.css:523`), `#c0392b`
  (`CellDialog.module.css:102`, `ConfirmDialog.module.css:49-57`), dark `#ff8a80` vs `#ff6b6b` —
  and `frontend.md` says new colours are tokens, but there is no `--danger` token to use.
- [ ] **R28** Rust duplication: `build_where` + `escape_like` (`mysql.rs:301-392` /
  `postgres.rs:422-547`), `split_statements` (`mysql_script.rs:67-185` / `postgres_script.rs:91-217`,
  plus the TS port — three implementations of one grammar, see R7), `StatementResult`/`SqlProblem`/
  `ServerInfo` declared per driver, `column_value` ×2, `in_background` (`secrets.rs:64`,
  `commands/mod.rs:434`), `app_data_dir` (`commands/mod.rs:446`, `terminal/commands.rs:102`),
  `CREATE_NO_WINDOW` + `creation_flags` ×4 (`dump.rs:293,579`, `tools.rs:541`, `local.rs:118`).
  The last three are a ten-minute helper.
- [ ] **R29** Dump/restore cannot be cancelled and the child is never killed:
  `dump.rs:271-352` (`run`), `commands/mod.rs:434-442` (`in_background` = `spawn_blocking`).
  Closing the tab or `disconnect_db` mid-transfer leaves `mysqldump`/`pg_dump`/`mongorestore`
  running to completion and `transfer://progress` firing for a dead id. Needs a `Child` handle
  and a cancel token like `rest_cancel`.
- [ ] **R30** Terminal output has no backpressure: `local.rs:270`, `remote.rs:28`
  (`mpsc::unbounded_channel`); `stream.rs` bounds the frame, not the total in flight. `cat` of a
  multi-GB file, or `yes` while the webview paints slower than the pty, grows memory without
  limit. A bounded channel and let the pty reader block.
- [ ] **R31** `ssh/mod.rs:106-116` (`remember_host`) writes `known_hosts.json` with
  `std::fs::write` (truncate then write, no temp+rename) inside a read-modify-write with no lock:
  a crash mid-write blanks the file and TOFU silently re-accepts every host; two first
  connections at once lose one entry. Blocking I/O in async in the same area (`:279,293,359`),
  `terminal/commands.rs:46` (`local::spawn` → ConPTY/openpty on the async worker) and
  `tools.rs:280-310` (`locate` walks PATH before `in_background`); `secrets.rs:64` is the model to
  copy.
- [ ] **R32** `dump.rs:816,965` (`--dbname={database}`), `:674,719` (positional db name for
  mysqldump/mysql). libpq reads a `dbname` containing `=` or starting `postgresql://` as a
  connection string; mysqldump reads a leading `-` as an option. `PGDATABASE` (or a URI-encoded
  `--dbname`) and a `--` before the MySQL name.
- [ ] **R33** `db/models.rs:13-36` (`ConnectionConfig.password`, `uri`), `ssh/mod.rs:14-30`
  (`SshAuth::Password`, `passphrase`), `terminal/models.rs:22` all `derive(Debug)`. Nothing prints
  them today; a manual `Debug` that redacts keeps it that way.
- [ ] **R34** `db/state.rs:61` (`running_queries: HashMap<String, u64>`) and
  `commands/mysql.rs:388-408,433-439`: cancel is keyed by connection id, so two scripts on one
  connection overwrite each other and Cancel kills the wrong one. Invisible today with one
  `QueryEditor` per workspace; "multiple query tabs" is on the roadmap and would expose it.
- [ ] **R35** `tools.rs:770-776,814` (`install`): the `download-{uuid}` staging directory is only
  removed at the end of the function; a crash leaves hundreds of MB and nothing sweeps
  `download-*` at startup.
- [ ] **R36** Test gaps in pure logic. TypeScript, no `*.test.ts`: `db/sql/lint.ts` (697 lines,
  the tokenizer), `db/filters.ts` (`splitFilterList` claims to mirror Rust `split_list`),
  `db/redis/keyTree.ts`, `db/mongo/{docOps,bsonTypes,filters}.ts`, `db/sql/{syntax,schemaCache}.ts`,
  `db/postgres/{columns,editing}.ts` (mysql has them), `db/mysql/editing.ts`, `rest/domTree.ts`,
  `components/JsonView/tokens.ts`, `components/TabStrip/slide.ts`, `core/{paneCache,textEntry,platform}.ts`.
  Rust: `mysql_structure::{column_definition,add_index_clause,default_clause,validated_collation}`
  (`:310-463`; the PG twins are tested), `mongo::{build_filter,parse_value,parse_regex,escape_regex}`
  (`:565-763`), `postgres::{placeholder,key_predicate}` (`:844-872`), `dump::split_uri` with `@`/`?`
  in the password (`:1000-1022`), `redis::list_databases`. Plus the cases R7 and R12 name.
- [ ] **R37** `rest/RestTab.tsx:352-445` (`send`): closing the tab mid-request lets `rest_send`
  run to `timeoutMs` (up to 600 s) and still `recordSend`. A cleanup that `restCancel`s the
  in-flight ids is enough.
- [ ] **R38** Shell hardening, all small: `shell/session.ts:113-119` (`writeSession`) is called
  from an effect (`App.tsx:122-124`) with no try/catch — a `QuotaExceededError` or an
  unserialisable module slot crashes the app on every render, while `parseSession` on the read
  side is fully defensive; `shell/theme.ts:111-113` applies platform/accent/glass at import but
  not `applyTheme(readStoredTheme())`, so the theme depends entirely on `public/theme-preload.js`
  loading; `core/shortcuts/decide.ts:83-91` "last registered wins" relies on React effect order
  and on `rest.closeRequest` becoming `enabled` later than `app.closeTab` — worth a comment on
  `useShortcut`; `terminal/i18n/{en,vi}.ts` are not annotated with a `TerminalDict` type, unlike
  `DbDict`/`RestDict`, so a missing key there is not a compile error (the dictionaries happen to
  match today: 1112/1112).
- [ ] **R39** Hygiene: actions on floating tags (`tauri-apps/tauri-action@v0`, `rust-cache@v2`,
  `checkout@v4`, `setup-node@v4`) in a workflow with `contents: write` and the updater signing
  key — pin at least tauri-action to a SHA; no `LICENSE` and no `license` field on a public repo
  with public releases; CI Node 20 vs dev Node 24 with no `engines`/`.nvmrc`; `tsconfig.json:24`
  references `tsconfig.node.json` but `build` runs `tsc` without `-b`, so `vite.config.ts` is never
  type-checked; two `reqwest` majors compile (0.12 direct, 0.13 via a plugin) against the "one
  TLS stack" note at `Cargo.toml:45`; `.gitignore` lacks `*.tsbuildinfo` and `.claude/`;
  `CHANGELOG.md` uses `## [0.0.18]` headings without the link definitions the format expects;
  `README.md:18-51` restates the changelog by hand; `capabilities/default.json` grants
  `opener:default` (includes `reveal-item-in-dir`) where `allow-open-url` is all that is used;
  the frontend is one 1.8 MB chunk (CodeMirror, xterm, sql-formatter could be dynamic imports).

## Done well

- **R40** The module boundary is real: `modules/` appears in `shell/`, `core/`, `components/`,
  `icons/`, `i18n/` only at `shell/registry.ts` and `i18n/dicts.ts`. No `any`, `as unknown as` or
  `!` in the shared layer; two `as` casts in the modules outside tests, one of them R17.
- **R41** SQL injection is closed the same way everywhere: `quote_ident` (`mysql.rs:98`,
  `postgres.rs:164`), values always bound, filter/sort columns checked against the catalogue
  before they reach SQL (`mysql.rs:316-322,528-531`, `postgres.rs:461,766`), PG cast types from
  `format_type` (`postgres.rs:844-849`), access methods and collations whitelisted
  (`postgres_ddl.rs:92-103`, `mysql_structure.rs:335-346`), DDL through prepared statements so a
  `;` cannot become a second statement. Both `build_where`s have SQLi tests.
- **R42** Credentials never reach a command line: `dump.rs:52-85` (`OptionFile`,
  `--defaults-extra-file`, 0600), `:94-143` (`PgPassFile`, `PGPASSFILE`, `--no-password`), removed
  in `Drop`; tool downloads pinned by SHA-256 and verified before extraction (`tools.rs:379-478`).
- **R43** Connection maps are never held across an `await` (`commands/mod.rs:295-303`,
  `:385-411`, `:274-287`) — exactly what `backend.md` promises. `Tunnel::drop` aborts the task
  (`ssh/mod.rs:230-235`), `RETRY_COOLDOWN` protects sshd, keepalive/backoff have tests, and TOFU
  refusals come back with old/new fingerprints and the file path (`:259-301,343-350`).
- **R44** `retry_read!` applies to reads only with a test pinning the attempt count
  (`commands/mod.rs:34-43,457-498`); `lost_connection` separates transport from server errors with
  tests (`mysql.rs:951-979`); `AppError` is an i18n key + params with the driver message attached.
- **R45** Terminal ordering — bytes drained before `Exit` (`local.rs:330-358`, `remote.rs:96-103`),
  the batcher tested under `start_paused` (`stream.rs:71-152`), `keys.ts` `shellKeeps` asking the
  real shortcut catalogue, ids minted inside the effect so StrictMode closes the right session.
- **R46** The async guards in the newer frontend code: `schemaCache.ts:479-541` generations +
  in-flight identity, `TunnelBanner.tsx:57-74` `stopped` before `listen()` resolves,
  `QueryEditor.tsx:215-238` draft restore (`restoring` ref, save-on-cleanup, yield to typing),
  `SqlTable.tsx:337-364` / `NoSqlTable.tsx:275-309` swapping state during render so a fetch never
  leaves with the old table's page. R13 is the older code that predates this pattern.
- **R47** `core/shortcuts/decide.ts` pure and fully tested; `useShortcut` latest-ref so handlers
  never go stale; `store.ts:49-57` idempotent `enterModal` disposer; `tabs.ts` identity bail-outs
  with the loop explained and locked by tests; `session.ts`, `theme.ts` and `i18n/index.tsx`
  validate everything read from localStorage; `i18n/dicts.ts:43-51` `Collision` makes a
  duplicated group a compile error.
- **R48** REST: `interpolate.ts` (`FROZEN`, `MAX_PASSES` cycle detection), `resolveRequest.ts`
  never writes back, secrets kept in braces in history; the Rust side caps `MAX_BODY` while still
  counting `body_size`, cancels by dropping the future, caches clients by `(follow, insecure)`;
  `HtmlPreview` never offers `allow-same-origin` and says why in the file.
- **R49** Release tooling: `release.yml:97-115` passes the changelog section through
  `$GITHUB_OUTPUT` with a random heredoc delimiter and never through a shell;
  `update-notes.yml` + `sync-notes.mjs` idempotent and queue-not-cancel; `set-version.mjs` cuts
  the changelog before touching any file, throws when a regex misses, is idempotent; production
  CSP has no `'unsafe-inline'` for scripts and the reason is written in three places.
- **R50** `.agent/` itself: the changelog convention, the plans/specs split and the
  adding-a-command five-places list all match the code and each other; the roadmap note keeps
  the *reasoning* behind reversed decisions (cell → row selection) rather than just the outcome.
